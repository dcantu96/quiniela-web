<div class='p-6 container mx-auto'>
  <p>Torneo: <%= @group.tournament.name %></p>
  <%= link_to 'Editar', edit_admin_tournament_path(@group.tournament), class: 'btn btn--primary' %>
  <p>Año: <%= @group.tournament.year %></p>

  <% if @group.tournament.weeks.count != 0 %>
    <p>Cantidad de semanas: <%= @group.tournament.weeks.count %></p>
    <%= button_to 'Actualizar Semanas', update_week_matches_admin_tournament_path(@group.tournament), class: 'btn btn--primary' %>
  <% else %>
    <p>No hay semanas. Crear semanas</p>
    <%= button_to 'Crear Semanas', generate_week_matches_admin_tournament_path(@group.tournament), class: 'btn btn--primary' %>
  <% end %>
</div>